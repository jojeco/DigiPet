# Next Increments

- **Persist points in AsyncStorage** — points reset on app restart; save/load alongside happiness and hunger so the economy survives sessions.
- **Hunger warning indicator** — show a visual cue (color change or icon) when hunger drops below 30 so players know the 2x happiness-decay penalty is active.
- **Shop items persist / cooldowns** — prevent the Toy inventory interval from fighting with shop purchases; consider removing the auto-add interval now that Toys are buyable in the shop.
- **Stat bars instead of raw numbers** — replace the plain `Text` displays for happiness and hunger with animated progress bars for a more polished, game-like feel.
